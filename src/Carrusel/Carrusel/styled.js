import Box from "@mui/material/Box";
import styled from "styled-components";

const CarruselBox = styled(Box)({
    widht: '100%',
    padding: '10px',
    display: 'flex',
    flexDirection: 'column',
    rowGap: '10px',
    border: '10px solid red'
});

export default CarruselBox;